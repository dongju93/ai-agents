[project]
name = "warm-up"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.102.0",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.4.6",
]
